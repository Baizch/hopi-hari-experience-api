export interface IAttraction {
  category: string;
  name: string;
  description: string;
  minHeight: string;
  maxHeight: string;
  status: string;
  openingHours: string;
  image: string;
  altImage: string;
  video: string;
  observation: string;
}
